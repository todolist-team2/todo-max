import { styled } from "styled-components";

const Dim = styled.div`
  width: 100%;
  height: 100%;
  position: absolute;
  left: 0;
  top: 0;
  background-color: rgba(20, 33, 43, 0.3);
`;

export default Dim;
